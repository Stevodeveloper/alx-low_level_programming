#include "main.h"

/**
 * _abs - computes the absolute value
 * @r: receives integer
 * Return: Always 0 (Success)
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
	return (0);
}
