#include "main.h"

/**
 * mul - multiply two numbers
 * @a: receive integer
 * @b:receive integer
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;
	_putchar(prod);
	return (0);
}
